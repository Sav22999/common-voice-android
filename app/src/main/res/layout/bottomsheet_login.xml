<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:viewBindingIgnore="true"
    tools:layout_gravity="bottom"
    android:id="@+id/verificationLinkSectionBottom"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/top_border_radius_with_border"
    android:paddingStart="20dp"
    android:paddingTop="0dp"
    android:paddingEnd="20dp"
    android:paddingBottom="20dp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent">

    <View
        android:id="@+id/viewSeparatorVerificationLink"
        android:layout_width="60dp"
        android:layout_height="6dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/top_bottom_border_radius"
        android:backgroundTint="@color/colorWhite"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/textURLVerificationLink"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/txt_rounded_darktheme_with_border"
        android:ems="10"
        android:gravity="center_vertical"
        android:hint="@string/txt_verification_link"
        android:inputType="textUri"
        android:maxLines="3"
        android:paddingStart="16sp"
        android:paddingTop="16sp"
        android:paddingEnd="20sp"
        android:paddingBottom="16sp"
        android:singleLine="false"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:textColor="@color/colorWhite"
        android:textColorHint="@color/colorGray"
        android:textCursorDrawable="@null"
        android:textSize="@dimen/text_normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/viewSeparatorVerificationLink"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>